apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: demo-service
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: demo-service
  endpoints:
    - port: http
      path: /metrics
      interval: 10s
