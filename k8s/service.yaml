apiVersion: v1
kind: Service
metadata:
  name: demo-service
  labels:
    app: demo-service
spec:
  type: ClusterIP
  selector:
    app: demo-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5050
