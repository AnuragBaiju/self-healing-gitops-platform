apiVersion: v1
kind: Pod
metadata:
  name: traffic-generator
spec:
  containers:
    - name: traffic-generator
      image: busybox
      command:
        - sh
        - -c
        - "while true; do wget -q -O- http://demo-service.default.svc.cluster.local/ > /dev/null; sleep 0.5; done"
